<!DOCTYPE html>

<title>About</title>

{% block body %}
    <h1>About</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ about.content }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ about.updated ? about.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('about_index') }}">back to list</a>

    <a href="{{ path('about_edit', {'id': about.id}) }}">edit</a>

    {{ include('about/_delete_form.html.twig') }}
{% endblock %}
