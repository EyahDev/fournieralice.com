{% extends 'base/base.html.twig' %}

{% block body %}
    <p>Réinitilisation de votre mot de passe</p>
    {% for label, messages in app.flashes  %}
        {% for message in messages  %}
            <p>{{ label }} : {{ message }}</p>
        {% endfor %}
    {% endfor %}
    {{ form(form) }}
{% endblock %}